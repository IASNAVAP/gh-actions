name: 01-building-blocks

on: workflow_dispatch

jobs:
    echo:
      runs-on: ubuntu-Latest
      steps: 
        - name: print name
          run: |
           echo "test cases are passed"


      
