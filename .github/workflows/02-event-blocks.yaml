name: 02-event-blocks

on: 
    workflow_dispatch:

jobs:
    echo:
        runs-on: ubuntu-Latest
        steps:
            - name: print event
              run: |
               echo "event is ${{github.event_name}}"
